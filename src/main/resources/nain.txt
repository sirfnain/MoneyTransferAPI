<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        .container {
            display: grid;
            grid-template-rows: 50px auto 50px;
            grid-template-columns: 20% 80%;
            min-height: 100vh;
            padding: 10px; /* Added padding */
        }

        .header {
            grid-row: 1;
            grid-column: 1 / span 2;
            background-color: #f2f2f2;
            text-align: center;
            line-height: 50px;
            overflow: hidden;
        }

        .header h1 {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            padding-right: 30px;
        }

        .lookup-btn {
            margin-left: 10px;
            padding: 5px 10px;
            background-color: #4CAF50;
            color: #ffffff;
            border: none;
            cursor: pointer;
        }

        .column1 {
            grid-row: 2;
            grid-column: 1;
            background-color: #e6e6e6;
            padding: 20px;
        }

        .column2 {
            grid-row: 2;
            grid-column: 2;
            background-color: #ffffff;
            padding: 20px;
        }

        .footer {
            grid-row: 3;
            grid-column: 1 / span 2;
            background-color: #f2f2f2;
            text-align: center;
            line-height: 50px;
        }

        .notification {
            display: block;
            background-color: #ffdddd;
            color: #ff0000;
            padding: 10px;
            margin-bottom: 10px;
            position: relative;
        }

        .notification .close-btn {
            position: absolute;
            top: 5px;
            right: 5px;
            cursor: pointer;
            font-weight: bold;
        }

        .form-group {
            margin-bottom: 10px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        .form-group input {
            width: 90%;
            padding: 5px;
        }

        .btn {
            background-color: #4CAF50;
            color: #ffffff;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Header Text That Is Quite Long</h1>
        </div>
        <div class="column1">
            <h2>Column 1</h2>
            <!-- Add content for column 1 here -->
        </div>
        <div class="column2">
            <h2>Column 2 - Signup</h2>
            <div class="notification" id="error-notification">
                <span class="close-btn" onclick="hideErrorNotification()">&times;</span>
            </div>
            <form id="signup-form">
                <div class="form-group">
                    <label for="username">Username:</label>
                    <input type="text" id="username" name="username" required>
                    <button type="button" class="lookup-btn">Lookup</button>
                </div>
                <div class="form-group">
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password" required>
                </div>
                <div class="form-group">
                    <label for="confirm-password">Confirm Password:</label>
                    <input type="password" id="confirm-password" name="confirm-password" required>
                </div>
                <button type="submit" class="btn">Sign Up</button>
            </form>
        </div>
        <div class="footer">
            <h3>Footer</h3>
        </div>
    </div>

    <script>
        // JavaScript code for handling form submission and displaying notification
        document.getElementById('signup-form').addEventListener('submit', function(event) {
            event.preventDefault(); // Prevent form submission

            var password = document.getElementById('password').value;
            var confirmPassword = document.getElementById('confirm-password').value;

            if (password !== confirmPassword) {
                showErrorNotification('Passwords do not match!');
            } else {
                // Submit the form or perform other actions
                alert('Sign up successful!');
            }
        });

        function showErrorNotification(message) {
            var errorNotification = document.getElementById('error-notification');
            errorNotification.innerHTML = '<span class="close-btn" onclick="hideErrorNotification()">&times;</span>' + message;
            errorNotification.style.display = 'block';
        }

        function hideErrorNotification() {
            var errorNotification = document.getElementById('error-notification');
            errorNotification.style.display = 'none';
        }
    </script>
</body>
</html>
